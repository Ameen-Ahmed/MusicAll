import { ResultDirective } from './result.directive';

describe('ResultDirective', () => {
  it('should create an instance', () => {
    const directive = new ResultDirective();
    expect(directive).toBeTruthy();
  });
});
